/* node.h */
#ifndef NODE_H
#define NODE_H

// typedef int NodeEl;
// typedef struct node* Address;
// typedef struct node {
//     NodeEl info;
//     Address next;
// } Node;

// #define INFO(p) (p)->info
// #define NEXT(p) (p)->next

// Address newNode(NodeEl val);

#endif